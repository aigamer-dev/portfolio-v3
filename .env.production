# Production environment variables
# API calls will use proxy or fallback to profile.json
REACT_APP_API_URL=https://api.aigamer.dev
# API key should be set via GitHub Secrets or omitted to use profile.json fallback
